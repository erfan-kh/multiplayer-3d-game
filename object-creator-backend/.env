# MongoDB should still point to localhost since it's accessed by your backend server directly
MONGO_URI=mongodb://127.0.0.1:27017/object-creator

# React frontend should use your laptop's local IP to reach the backend from other devices
REACT_APP_API_BASE_URL=http://192.168.1.109:5000
